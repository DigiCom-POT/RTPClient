package digicom.rtpclient;


public class TopMovieWebClient {
	/**
	 * @param args
	 */
	public static void main(String[] args) {
	/*	setPort(6767);
		//staticFileLocation("/webapp");
		
		get("/ping", (req, res) -> "ping successful ....");
		
		
		get("/getTopMovies", (request, response) -> {
			response.header("Access-Control-Allow-Origin", "*");
			Gson g = new Gson();
			return g.toJson(CassandraConnector.getSortedTopMovies());
		});
		
		get("/getMovies", (request, response) -> {
			response.header("Access-Control-Allow-Origin", "*");
			Gson g = new Gson();
			return g.toJson(CassandraConnector.getTopMovies());
		});
		
		get("/close", (req,res) -> {
			CassandraConnector.close();
			return "Done ....";
		}) ;*/

	}
}
